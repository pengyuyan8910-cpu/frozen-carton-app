# 标准底表

这里始终只保留一份可继续编辑的标准底表：

`整箱到店数据测算_当前版.xlsx`

- 在 Excel 手工调整后：以相同文件名覆盖上传到本目录。
- 小程序新增门店、全店上新或调整陈列后：在小程序“导入导出”下载 `整箱到店数据测算_程序回传.json`，上传到 `data/inbox`。系统复核通过后，会自动把回传结果写回本目录这份 Excel。

请勿在本目录上传 JSON，也不要自行上传带其他名称的 Excel，避免产生多个底表来源。
