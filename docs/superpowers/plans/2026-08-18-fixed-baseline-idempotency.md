# 固定正式基准与幂等重排实施计划

1. 先用 `test-replan-idempotency.mjs` 证明当前代码缺少“同产品池直接复用正式方案”能力。
2. 新增纯函数 `replan-idempotency.mjs`，判断正式有效池是否与当前有效池一致，并构造正式方案复用结果。
3. UI 在全店/指定门店入口先判断幂等条件；成立时不调用业务优化器和严格排柜器，直接生成正式基准草稿；不成立才进入现有增量算法。
4. 用已确认 `整箱到店数据测算_最新产品池_20260817.xlsx` 替换 `data/source/整箱到店数据测算_当前版.xlsx`，通过现有 refresh-data/verify 生成正式 app-data。
5. 回归验证：当前产品池67；宁国67/67、343.7L；和县67/67、614.6L；无为鼓楼64/67、541.2L；全部门店正式基准重复重排不漂移。
6. GitHub CI与正式底表校验通过后才合并master。
