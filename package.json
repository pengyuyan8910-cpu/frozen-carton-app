{
  "name": "frozen-carton-unified-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh-data": "node scripts/refresh-data.mjs",
    "verify:data": "node scripts/verify-app-data.mjs",
    "check": "node --check app.js && node --check bootstrap.js && node --check scripts/refresh-data.mjs && node --check scripts/verify-app-data.mjs && node --check scripts/source-to-app-data.mjs && node --check scripts/app-data-to-workbook.mjs && node --check scripts/generate-new-store-draft.mjs",
    "generate-new-store-draft": "node scripts/generate-new-store-draft.mjs"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
