# 统一 SKU 与产品生命周期固定规则 Skill

## 1. 适用范围

本规则适用于“冻品整箱到店统一小程序”中的以下模块：

- 门店执行
- 商品明细
- 排柜调整
- 陈列图
- 产品生命周期管理
- 变更任务中心
- 云端协作与本地运营草稿

所有模块必须读取同一套 SKU 身份、有效 SKU 池和生命周期完成状态，不允许各模块自行计算出互相冲突的结果。

## 2. 不可突破的原则

1. **默认只允许修改读取、识别、统计和展示逻辑。**
2. **未经用户明确说“允许修改数据”，禁止删除、合并、清理、迁移或覆盖以下原始数据：**
   - `skus`
   - `productPool`
   - `draftProducts`
   - `tasks`
   - `slots`
   - `committedPatches`
   - 本地草稿、发布数据和云端数据
3. 发现历史异常数据时，先通过只读投影兼容；不得以“修复”为名静默改写原始记录。
4. 任何数据迁移必须先向用户列明：影响对象、影响行数、修改前后样例、回滚方案，并获得明确授权。
5. 老板块和新板块不得各自维护独立的产品池或状态口径。

## 3. SKU 唯一身份规则

### 3.1 正常身份

- 有有效条码时：条码为第一唯一键。
- 没有有效条码时：规范化商品名称为备用唯一键。
- 有效条码格式：6—18 位纯数字。

### 3.2 历史字段颠倒兼容

发现以下历史异常时，只在读取层纠正：

- `name` 是纯数字条码；
- `barcode` 是商品中文名称。

读取结果应解释为：

- 规范条码 = 原 `name`；
- 规范商品名 = 原 `barcode`。

不得直接改写原始记录。

### 3.3 合并展示规则

- 规范条码相同：展示、统计为同一个 SKU。
- 一条有条码、一条无条码，但规范商品名相同，且只能对应一个条码：展示、统计为同一个 SKU。
- 两条记录均有不同有效条码，即使名称相同，也不得自动合并。
- 合并只生成临时读取副本，不得删除任何原始行。

## 4. 统一数量口径

同一数据源、同一门店范围下必须满足：

> 门店执行有效 SKU 数 = 商品明细有效 SKU 数 = 产品生命周期有效 SKU 数

计算有效 SKU 时：

- 使用统一规范唯一键去重；
- 新品草稿不计入有效 SKU；
- 淘汰完成商品不计入有效 SKU；
- 正常在售、上新完成、恢复完成商品计入有效 SKU；
- 任务未完成不得擅自增加或减少有效 SKU。

出现数量不一致时，先检查身份识别和统计范围，不得通过删除数据强行对齐。

## 5. 商品状态与任务状态必须分离

### 5.1 商品生命周期只允许以下状态

- `正常在售`
- `新品草稿`
- `上新完成`
- `淘汰完成`
- `恢复完成`

产品总池、商品详情和任务中心的“商品当前状态”中，禁止使用：

- `待上新`
- `待淘汰`
- `恢复中`

### 5.2 完成结果映射

- 上新任务状态为 `已完成` → 商品状态显示 `上新完成`。
- 淘汰任务状态为 `已完成` → 商品状态显示 `淘汰完成`。
- 恢复任务状态为 `已完成` → 商品状态显示 `恢复完成`。

### 5.3 未完成任务的展示

任务是否尚未执行、部分完成或执行完毕，只能在任务中心的“任务执行状态”中展示：

- `待执行`
- `部分完成`
- `已完成`
- `已撤销`
- `已撤回`

未完成任务不得把商品当前状态改成“待上新”或“待淘汰”。

陈列图需要表达未完成动作时，应使用：

- `上新任务执行中`
- `淘汰任务执行中`
- `恢复任务执行中`

不得再次使用“待上新”“待淘汰”“恢复中”作为商品状态。

## 6. 新增商品防重规则

保存新品草稿前必须只读检查：

1. 规范条码是否已存在；
2. 无条码商品的规范名称是否已存在；
3. 是否存在 name/barcode 颠倒但实际身份相同的历史记录。

命中重复时必须阻止本次重复创建并提示用户，不得自动覆盖原商品，也不得自动删除已有草稿。

## 7. 联动顺序

一次生命周期动作必须按以下顺序联动：

1. 创建任务；
2. 任务中心展示执行状态；
3. 任务完成；
4. 生成完成结果；
5. 老板块和新板块重新读取统一有效 SKU 池；
6. 门店执行、商品明细、陈列图和生命周期管理同步刷新。

禁止出现：任务已经完成，但商品状态仍显示待执行类状态；也禁止新模块显示一个数量、旧模块显示另一个数量。

## 8. 修改边界

### 默认允许

- 修正只读身份识别；
- 修正统计口径；
- 修正页面状态文字；
- 增加重复创建拦截；
- 增加回归校验；
- 修改缓存版本号。

### 未经授权禁止

- 删除重复 SKU 原始行；
- 自动合并 `productPool`；
- 清空生命周期任务；
- 清空本地存储；
- 改写云端数据；
- 修改商品名称、条码、尺寸、箱规、陈列位置或坑位；
- 将历史任务重新执行。

## 9. 每次发布前的强制回归检查

1. 同一商品正常字段与颠倒字段只能展示一条。
2. 两个不同条码的同名商品仍展示两条。
3. 门店执行与生命周期有效 SKU 数一致。
4. 完成上新任务后显示“上新完成”。
5. 完成淘汰任务后显示“淘汰完成”。
6. 完成恢复任务后显示“恢复完成”。
7. 产品总池不存在“待上新”“待淘汰”“恢复中”状态。
8. 修改前后原始 `skus/productPool/draftProducts/tasks/slots` 数组长度和内容保持不变，除非用户明确授权数据写入。
9. 页面刷新、切换门店、云端拉取后仍保持相同口径。
10. 新增重复商品时必须在保存前被拦截。

## 10. 本次已知回归样例

以下两组字段颠倒记录必须识别为同一商品：

- `精品榴莲芝士披萨180g` / `6937506894553`
- `奥尔良风味鸡肉披萨200g` / `6937506894564`

无论字段顺序是否颠倒，均不得重复展示、重复计数或生成第二条上新商品记录。
